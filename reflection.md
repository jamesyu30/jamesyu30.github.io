1. The browser sends a GET request to the root directory first (/) and then it sends another GET request to the css file to style the website. Finally, it sends another GET request to get the image in the assets folder.

2. A docker container is an isolated environment that contains libraries and other dependencies and their versions so that a project can be run in any enviroment and get the same result each time. Github pages uses pre-built files that only runs in the way github intends to run them. 

--- 

1. Flex direction changes the way the items in a flex box are organized. If the flex direction is, the contents will go next to each other in a line. If the flex direction is a column, then the items will stack on top of each other.

2. It is important to use relative units to make a website more accessible and good to look at with different screen sizes. It also makes sure that websites maintain functionality at smaller screen sizes. With fixed units, the sizes don't change so the size that looks good for one screen might not look as good for another screen.